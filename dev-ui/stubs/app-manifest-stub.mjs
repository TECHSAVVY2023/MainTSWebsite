/**
 * Satisfies Vite’s static analysis of `import("#app-manifest")` in Nuxt’s
 * manifest composable when `experimental.appManifest` is disabled (default).
 * Do not use this file if you turn on `experimental.appManifest` — remove the
 * matching alias from `nuxt.config.ts` instead.
 */
export default {}
